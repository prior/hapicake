from setuptools import setup

VERSION = '0.1.0'

setup(name='hapicake',
  version=VERSION,
  description='a better api wrapper',
  author='Prior',
  author_email='mprior@hubspot.com',
  url='http://hubspot.com/',
  packages=['cake'],
  install_requires=[],
  dependency_links=[],
  platforms=['any'],
)
